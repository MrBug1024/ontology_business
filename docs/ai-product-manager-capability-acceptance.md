# AI 产品经理能力建设与验收

日期：2026-09-07 至 2026-09-08。状态：技术回归通过，业务质量复测中；开发态配置尚未正式发布。

2026-09-08 用户纠正职责边界后，本方案已退役。本文件保留旧方案与通用缺陷的历史证据；当前重建与交付边界见 `docs/project-lifecycle-capability-acceptance.md`。旧任务成功不代表第三方项目协作流程已完成。

## 需求账本

| ID | 用户目标与可观察验收 | 影响范围 | 保持不变项 | 证据状态 |
| --- | --- | --- | --- | --- |
| PM-01 | 专业咨询、痛点与需求分析；区分来源、假设、已定范围和建议 | 场景工作流与验证 Agent 配置 | 只使用本次显式输入；不虚构客户事实 | 首次实测发现无依据工期与来源，已修正指令，待复测 |
| PM-02 | 多项目台账分析、基线对实际、资源冲突、进度跟进行动 | 多项目组合与进度复盘工作流 | 当前按本次台账分析；不宣称已有长期项目存储、系统同步或自动监控 | 已配置；Agent 预演被数据库状态长度错误阻断 |
| PM-03 | 需求材料经过分析、人工核对，交付可下载 Word PRD | PRD 模板、模板附件 Action、需求分析与人工评审工作流 | 文件生成不代表批准排期或上线；确认和幂等保留 | 模板两页渲染检查通过；首份生成内容已驳回，修订版执行中 |
| PM-04 | 需求评分透明且缺项拒绝 | 现有加权评分函数输入契约 | 保留原五项权重；不把分数当概率或正式决策 | 五项均设必填，待实际计算验证 |
| FIX-01 | 预演在 PostgreSQL 持久保存；确认执行一次；重复确认重放同一回执 | ORM、Alembic、部署验证、PostgreSQL 回归 | 状态 token、租户、ACL、确认、幂等与协议语义不变 | 已复现 SQLSTATE 22001：awaiting_confirmation 超过 varchar(20) |
| FIX-02 | 验证 Agent 展示统一能力回执，用户确认后真正提交任务，并可从聊天打开任务 | Agent 确认协议、统一运行适配器、独立聊天确认组件 | 服务端恢复原始输入与身份；复用 CapabilityInvoker；模型不能确认；重复点击不得重复执行 | 前端实测统一回执不匹配旧确认格式，没有确认按钮，待补齐 |
| FIX-03 | 人工审批恢复后使用已经审阅的节点输出生成文件 | 工作流执行器与任务编排、审批恢复回归 | 原定义、输入、审批与执行身份保持固定，禁止重新生成已审内容 | 实测同一任务审批前后模型输出不同，确认恢复重跑 LLM，待修复 |
| FIX-04 | 工作流产物在任务详情提供可用下载入口 | 任务详情、附件解析与链接 | 只接受受管文件身份；不信任模型给出的任意下载 URL | 文件已生成但任务页只有原始结构文本，待补齐 |
| FIX-05 | 嵌套对象和数组的能力输入可进入受治理操作/工作流 | 内置 Provider 输入转换与回归 | 使用既有 canonical JSON 契约；不放宽输入 Schema 或加密要求 | 真实 Agent 输入触发不可变嵌套对象无法 JSON 序列化，待修复 |
| FIX-06 | 确认令牌不进入模型上下文或新聊天工具结果 | Agent 模型回执投影、历史回执重授权与回归 | 正式调用回执与服务端确认契约保持不变；浏览器只提交调用和消息身份 | 实测模型直接复述旧回执中的确认令牌，需在模型边界剔除 |

影响图：前端/Agent/REST/MCP/SDK → 统一 CapabilityInvoker → capability_invocations.status → Alembic 与真实 PostgreSQL 回归；场景专业内容仅配置在场景定义、工作流和 Agent 中。

FIX-02 影响图：AgentChat → 独立能力回执组件/API/DTO → Agent 统一能力确认服务 → 原始持久对话与加密调用输入 → CapabilityInvoker → 任务/附件回执 → 协议、权限与前端回归。此项为真实前端闭环所必需，不引入行业执行旁路。

非目标：本轮不实现离线 Skill 导出、外部项目系统写入、自动通知或虚构项目实例。不修改已有账户与工作区治理改动，不改写历史迁移。

## 当前实测记录

- 原“基于证据的需求分析”操作实际绑定表格统计技能 data-analyzer，已停用并保留历史定义。改由专门的痛点发现工作流负责。
- Word 模板有 15 个必填变量，涵盖 13 个正文章节、标题和版本；已在前端注册并配置模板附件操作。
- 首份 PRD 流程到达人工审批后，发现虚构访谈来源、将确认前取消扩成事后撤销等范围错误，已记录理由并驳回，未生成该版本文件。
- Agent 项目报告的两次失败均无能力调用持久回执。只读核对后重试一次仍失败；事务内预演诊断复现 PostgreSQL 状态长度错误，诊断事务已回滚，未创建项目任务。
- 模板渲染使用隔离解包的 LibreOffice，未安装到全局环境；下载包 SHA-256 已验证。

## 验收结果

技术验证：

- `python -m pytest backend/tests -q --tb=short --durations=12`：1025 passed、14 skipped、2 warnings；1926.76 秒。日志 `.runtime/pm-backend-verified.log`。
- 令牌投影最后调整后的 Agent、确认与协议一致性专项：29 passed；日志 `.runtime/pm-token-boundary-tests.log`。
- 内置 Provider、操作、确认与协议专项：79 passed；日志 `.runtime/pm-final-targets.log`。
- `RUN_POSTGRESQL_INTEGRATION_TESTS=1` 下 `test_capability_confirmation_postgresql.py`：2 passed，包含嵌套输入、确认及重复确认只创建一个任务。
- `verify_alembic_roundtrip.py` 与 `verify_postgresql_runtime.py` 已通过；实际 single head 为本次新增的 `20260907_26`，运行角色保持无 DDL 权限。
- `npm --prefix frontend test`：103 passed；`npm --prefix frontend run build` 通过。大型 chunk 提示未改变运行结果。
- `git diff --check` 通过；已有换行符转换警告，无空白错误。

真实浏览器与产物：

- 聊天中点击确认、取消确认、刷新回执以及任务链接均已操作；能力确认由服务端执行，模型不负责确认。
- 多项目调用 `9fc59ad8d3984148aa21cbb4e36c2a83` 已真实创建并完成任务 `0a891910400f411cac3b97226c402378`，两个分析节点均成功。
- 发现另一个旧版本本机 8000 服务会消费相同队列；已核对进程并同步重启 8000/8001。此前样例不作为审批恢复修复的通过证据。
- Word 任务 `c466414f5b0743eaa1c7a0c2e0fca471` 审批后成功生成文件。审批前后 n2 结果 SHA-256 均为 `7acb79065879a419f7762d6599a3bcef3942c3f197242cee02694f42aa9e7b08`，没有重新调用模型改写已审内容。
- 真实下载文件 `fef8776a4a397ac67bd07eaceb1c8a24`，41466 字节，SHA-256 `3fc72492d4a00c5ffd985903c95f230a727201220eb671f031a4ea9a4780a15e`。渲染 5 页并逐页检查，无截断或重叠。
- Word 样例仅批准生成待评审文件。审批意见记录了认证与必填校验混用、新增反馈 ID 未标建议等内容缺陷，不代表业务质量或上线审批通过。
- 评分实测发现遗留必填数据集端口 `priority_factors` 与五项标量输入不相符，已通过前端退役，保留历史快照。五项均为 50 时真实调用返回 40.0；独立缺少 effort 的案例拒绝估分并要求补充。
- 痛点工作流已完成真实案例，但出现无依据成本、优先级和时间估计，正在收紧场景指令并复测。

当前边界：仅本次台账分析，没有长期项目登记、跨次自动更新、外部项目系统同步或自动跟踪通知。仍是 dev 定义，修改场景后旧 dev 任务可能因定义哈希变化不可从任务页读取；正式运行需要发布并固定不可变版本。未进行离线 Skill 打包或对外生产发布。配置保存、结构校验通过均不等同于业务验收通过。
