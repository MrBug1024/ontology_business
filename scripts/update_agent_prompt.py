import urllib.request, json

AGENT = "7eb71f4a9a49417693d79b95c67879d2"
r = json.load(urllib.request.urlopen(f"http://127.0.0.1:8001/api/agents/{AGENT}"))
sp = r["system_prompt"]

# 在 "### 4. 工作流程" 之前插入年度审计模块
audit_section = """### 4. 年度审计（年审）
- **业务流程**：签订审计业务约定书 → 风险评估与重要性水平 → 总体审计策略 → 具体审计计划 → 六大循环测试（销售/采购/存货/薪资/货币资金/固定资产）→ AI 抽凭+索引 → 函证程序（回函率≥70%）→ 函证结果分析 → 审计调整 → AI 生成审计报告 → 三级复核（项目-部门-主任）→ 出具正式报告 → 归档+客户回访
- **重要性水平**：小微/一般企业以资产总额为基准取 0.5%–1%；高新结合收入/研发费用加计扣除；IPO 三年一期+内控穿行测试；实际执行重要性通常为整体的 50%–75%
- **函证程序**：应收账款、银行存款必须函证；回函率≥70%，未达标执行替代程序（期后收款、合同、发票）并追加审计；函证差异逐笔核实形成审计调整
- **审计调整**：经客户确认后入账；客户拒绝且金额重大时评估对审计意见影响
- **审计意见**：标准无保留、保留意见、否定意见、无法表示意见；非无保留需与客户管理层重大事项沟通
- **AI 自动化**：AI 助理工程师负责抽凭抽样（≥70%）、底稿索引、测算表；AI 注册会计师负责风险评估矩阵、审计报告初稿、复核关注点识别、政策版本比对
- **可用操作**：查询审计项目、查询审计底稿、查询函证情况、查询审计调整
- **可用规则**：函证未回函预警、函证差异预警、高风险审计项目预警、审计调整未接受预警
- **可用工作流**：年度审计流程（查询项目→底稿→函证→调整→高风险判断→生成审计报告）

"""
sp = sp.replace("### 4. 工作流程", audit_section + "### 5. 工作流程")

# 更新工作流程描述，加入年审
sp = sp.replace(
    "凭证收集 → 票据归类 → 分录编制 → 勾稽校验 → 申报表生成 → 金额判断（<1000元直接申报，≥1000元客户确认）→ 申报回执 → 财务报表 → 客户沟通",
    "凭证收集 → 票据归类 → 分录编制 → 勾稽校验 → 申报表生成 → 金额判断（<1000元直接申报，≥1000元客户确认）→ 申报回执 → 财务报表 → 客户沟通\n年审：约定书 → 风险评估/重要性 → 审计计划 → 六大循环测试 → 抽凭 → 函证(≥70%) → 审计调整 → 生成报告 → 三级复核 → 出具报告 → 归档回访"
)

body = json.dumps({"system_prompt": sp}).encode()
req = urllib.request.Request(
    f"http://127.0.0.1:8001/api/agents/{AGENT}",
    data=body,
    headers={"Content-Type": "application/json"},
    method="PUT",
)
try:
    res = json.load(urllib.request.urlopen(req))
    print("updated, prompt length:", len(res.get("system_prompt", "")))
except Exception as e:
    print("ERR", e)
    print(e.read().decode() if hasattr(e, "read") else "")
